{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Backend",
      "program": "${workspaceFolder}/node_modules/.bin/nx",
      "args": ["run", "backend:serve"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
